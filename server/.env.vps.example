# Copie para .env na VPS:  cp .env.vps.example .env
# A senha DEVE ser a mesma do volume já criado (dhe-apk).

DHE_DB_PASSWORD=dhe-apk
JWT_SECRET=uma_senha_jwt_longa_e_aleatoria
PORT=4002
NODE_ENV=production

# NÃO coloque EXPO_PUBLIC_API_URL aqui — fica na raiz do app no PC:
# EXPO_PUBLIC_API_URL=http://195.35.40.86:8090
